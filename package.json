{
  "name": "pingdomcheck",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "setup": "npm install && npm run setup:all",
    "setup:all": "npm --prefix server install && npm --prefix client install",
    "dev": "concurrently \"npm run dev --prefix server\" \"npm run dev --prefix client\"",
    "start": "node server/index.js"
  },
  "devDependencies": {
    "concurrently": "^8.0.1"
  }
}
